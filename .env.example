# Copia como ".env" (gitignored) o ".env.local" para desarrollo.
# URL base de la API del backend (ver el repo fya-creditos-backend).
# En el emulador de Android, "localhost" es el propio emulador, no tu PC:
# usa 10.0.2.2 (emulador de Android Studio) o la IP de tu máquina en la red.
VITE_API_BASE_URL=http://localhost:5000
